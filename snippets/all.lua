--local ls = require "luasnip"
--print("this is coming from all.lua")

return {
  parse("trig", "loaded!")
  --all = {
    --ls.parser.parse_snippet("asdf", "-- this is what expanded asdf!"),
  --},
  --lua = {
  --}
}
